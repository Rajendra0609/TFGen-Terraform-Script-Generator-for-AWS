# ═══════════════════════════════════════════════════════════════════════════
# ⚠️  READ THIS BEFORE APPLYING  ⚠️
#
# 1. CRITICAL — ROTATE THE GMAIL CREDENTIAL NOW, SEPARATELY FROM THIS FILE:
#    The version of this manifest you gave me had a real Gmail address and
#    what is unmistakably a real Google App Password (16 lowercase letters,
#    4-4-4-4 grouped — that's the exact format Google issues) hardcoded as
#    SMTP_USER/SMTP_PASS. That credential has been exposed in a file that
#    was zipped up and shared around — treat it as compromised whether or
#    not you ever apply this manifest. Go to Google Account → Security →
#    App Passwords and revoke it, then generate a new one, right now, before
#    doing anything else here. I have NOT carried that value forward into
#    this rewritten file — SMTP_USER/SMTP_PASS below are inert placeholders.
#
# 2. Every other value below is a freshly-generated placeholder (generated
#    in this conversation, so — same as any value that's ever appeared in a
#    chat transcript — do not treat it as secret). It exists only so the
#    YAML is structurally valid enough to `kubectl apply` in a sandbox/test
#    cluster. Before any real deployment, generate your own and apply them
#    directly with kubectl instead of hand-editing base64 here — see
#    generate-secrets.sh alongside this file. Committing an edited version
#    of *this* file with real values into git is exactly the anti-pattern
#    that leaked the Gmail credential above; use generate-secrets.sh (or a
#    proper secret manager — Sealed Secrets / External Secrets Operator /
#    Vault) instead, and .gitignore this file if you do keep a local copy.
#
# 3. FIXED vs. the version you gave me:
#      • POSTGRES_PASSWORD was the literal string "tfgen123" — a weak,
#        guessable default. Replaced.
#      • JWT_SECRET and JWT_REFRESH_SECRET were IDENTICAL. That defeats the
#        purpose of having two separate signing keys — tests/unit/backend's
#        auth middleware tests specifically assert a refresh token can't
#        verify against JWT_SECRET, i.e. the app's own test suite assumes
#        these differ. Now two independently-generated values.
#      • REDIS_URL had no password at all (redis://tfgen-redis-svc:6379) —
#        Redis was reachable with zero authentication from anything that
#        could resolve that Service, i.e. every pod in the cluster. This is
#        the exact issue docker-compose.yml already fixed for the Compose
#        deployment; this Kubernetes manifest was regressing it. Added
#        REDIS_PASSWORD and wired requirepass into 04-redis-statefulset.yaml.
# ═══════════════════════════════════════════════════════════════════════════
apiVersion: v1
kind: Secret
metadata:
  name: tfgen-secret
  namespace: tfgen
  labels:
    app.kubernetes.io/name: tfgen
    app.kubernetes.io/part-of: tfgen
type: Opaque
data:
  # ── Postgres ────────────────────────────────────────────────────────────
  POSTGRES_USER:     dGZnZW4=
  POSTGRES_PASSWORD: bENUMmhkeEFST2VUVWZSWk1OV1FLYVQ1WnhYWGhELXo=   # PLACEHOLDER — replace
  POSTGRES_DB:       dGVycmFmb3JtX2dlbmVyYXRvcg==
  # Must stay in sync with POSTGRES_USER/POSTGRES_PASSWORD/POSTGRES_DB above —
  # if you regenerate one, regenerate this too (generate-secrets.sh does both).
  DATABASE_URL: cG9zdGdyZXNxbDovL3RmZ2VuOmxDVDJoZHhBUk9lVFVmUlpNTldRS2FUNVp4WFhoRC16QHRmZ2VuLXBvc3RncmVzLXN2Yzo1NDMyL3RlcnJhZm9ybV9nZW5lcmF0b3I=   # PLACEHOLDER — replace

  # ── Redis ───────────────────────────────────────────────────────────────
  # NEW — required by 04-redis-statefulset.yaml's `--requirepass`. Without
  # this, Redis runs with no auth at all (see warning above).
  REDIS_PASSWORD: NjNJZkltXzYtUzZvUnNkc1kxelMzNlN2VTQ5VTZlaFo=   # PLACEHOLDER — replace
  # Must embed the same password as REDIS_PASSWORD above.
  REDIS_URL: cmVkaXM6Ly86NjNJZkltXzYtUzZvUnNkc1kxelMzNlN2VTQ5VTZlaFpAdGZnZW4tcmVkaXMtc3ZjOjYzNzk=   # PLACEHOLDER — replace

  # ── Auth ────────────────────────────────────────────────────────────────
  # FIXED: these two used to be byte-for-byte identical. Now independently generated.
  JWT_SECRET:         ZDcwNjgxMzUwYzYyMzRhMGMyODgwNjkxYjk4ZDZkYzcyOTA3ZmFiMTc5ZmY4YWYwOGE2MTYxNzBkNDk5YjlhYQ==   # PLACEHOLDER — replace
  JWT_REFRESH_SECRET: YTY2Mzg5N2Q4ZjAxOWZkOWMyYWYzNjgzNDQwNTcxNzY3ZDAyNmQ1MTM2YjA3YjY4Njg4MmFjYWUxN2RkOWVkMQ==   # PLACEHOLDER — replace
  # Encrypts stored AWS credentials at rest (see backend/utils). Rotating
  # this invalidates every previously-stored credential — plan accordingly.
  ENCRYPT_KEY: NGZiZmRkMjZiODRjNWZkMDMyZmMwNGYwNjgwZjNjZjg0ZGY3MDg2NjAxMzlmMDA2ZDYyZjdmYzBmMDQ5ZDQ2ZA==   # PLACEHOLDER — replace

  # ── SMTP (nodemailer) ───────────────────────────────────────────────────
  # ⚠️ See warning #1 at the top of this file — do not reuse the credential
  # that was previously here. Use a fresh Gmail App Password (or better, a
  # transactional email provider's API key/SMTP creds) below.
  SMTP_USER: Q0hBTkdFX01FQGV4YW1wbGUuY29t                       # PLACEHOLDER — replace: CHANGE_ME@example.com
  SMTP_PASS: Q0hBTkdFX01FXzE2X2NoYXJfYXBwX3Bhc3N3b3Jk             # PLACEHOLDER — replace: CHANGE_ME_16_char_app_password
  SMTP_FROM: VEZHZW4gPENIQU5HRV9NRUBleGFtcGxlLmNvbT4=             # PLACEHOLDER — replace: TFGen <CHANGE_ME@example.com>

  FRONTEND_URL: aHR0cHM6Ly90Zmdlbi5leGFtcGxlLmNvbQ==              # https://tfgen.example.com — replace with your real domain
